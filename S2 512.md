# v5.12 S2：基于 contact event 的稀疏触球课程

本文只说明 v5.12 相对 [S2 510.md](S2%20510.md) 的主要变化。未特别说明的动作模仿、局部 reference command、观测维度和基础稳定性约束均沿用 v5.10。

版本范围：

- 基线：`efe34a1 Dribble v5.10`
- 当前版本：`47975c6 lab v5.12`
- S2 仍使用 reference root trajectory 计算的 pelvis-local `vx, vy, wz`，没有改成随机 command。
- 仍从 S1 checkpoint 继续训练，统一接口保持 163-D policy observation / 29-D action，不因 S2 课程切换改变网络形状。

## 总体变化

v5.10 用完整 reference clip、逐帧 CG 一致性和多项球位置/推进 shaping 共同训练盘带。v5.12 将目标缩小为：在 reference 指定的时间窗口内，用指定脚从指定左右侧完成一次新的真实物理触球。

| v5.10 | v5.12 |
|---|---|
| 整段 clip 上训练接触节奏和球流 | 按 1/2/4/8/完整 contact 序列采样短 episode |
| 逐帧 `cg_contact_consistency`、`cg_foot_consistency` | 每个 contact event 只判断 proximity、new touch、foot 和 side |
| 同时监督球靠近、推进、追球、释放和滑行等行为 | 两个 contact event 之间不监督球的位置、速度或完整轨迹 |
| `motion_foot_pos`、gait foot tracking、`foot_distance` 多套脚部约束并存 | 只保留一套 windowed foot tracking |
| 固定 no-touch、单帧 ball-lost、速度差等 termination | 用 missed-contact 和连续 ball-lost 代替 |

## Contact event 的定义

v5.12 从 motion 中的 `dribble_cg_contact` 上升沿生成 contact event。每个 event 保存：

- contact frame；
- 期望触球脚，`left=0`、`right=1`；
- 球在期望脚 yaw-local frame 中的左侧或右侧，`left=0`、`right=1`；
- event frame 上冻结的 reference foot position 和 reference foot yaw。

旧的 inside/outside instep 标签只用于换算左右侧：脚局部坐标的 `+Y` 为左侧、`-Y` 为右侧。换算完成后，reward 不再直接使用 inside/outside 名称。

每个 event 的有效时间窗口为 contact frame 前后各 `0.10 s`。目标区域定义在冻结的 reference foot yaw-local frame 中：

- 前后方向：`x ∈ [-0.06, 0.14] m`；
- 左侧：`y ∈ [0.04, 0.16] m`；
- 右侧：`y ∈ [-0.16, -0.04] m`；
- 脚中心左右各 `0.04 m` 为 dead zone，不判为正确侧。

S2 现在强制要求 motion 提供 `dribble_cg_contact`、`dribble_cg_foot`、`dribble_cg_surface` 和 `ball_pos_w`。当前仓库中兼容的数据目录是 `motions/master-single`。

## Episode 初始化和结束方式

短 contact 课程不再总是从 reference 第 0 帧开始：

1. 随机选择一个 contact event 或一段相邻 contact 序列；
2. 从第一个 event 前 `0.3–0.6 s` 的 reference state 初始化机器人；
3. 球放在第一个 event 的 reference 球位置，并增加 `5–8 cm` 的随机平面扰动；
4. 球从 reset 后完全由物理仿真驱动，`snap_mode="never"`；
5. episode 运行到最后一个 event 的窗口和短 grace period 结束，然后统一 reset robot、ball 和 RNN state。

因此 S2-A 的预期行为是“一次触球一个 episode”：每次随机抽一个 event，完成或错过该次触球后重置。用 S2-A task 执行 `play_multi.py` 时也仍然播放这种短 episode，不会自动恢复为完整 reference clip。`--disable_training_terminations` 只关闭失败 termination，也不会把 S2-A 改成 full-reference playback。

## Reward 的变化

### 保留的部分

v5.10 的 body position/orientation imitation、低权重 body velocity imitation、pelvis-local anchor linear/angular velocity tracking、action/waist rate、joint limit、pelvis upright、常规非期望身体接触和上肢正则继续保留。reference `vx, vy, wz` 的权重和标准差仍为：

- `motion_anchor_lin_vel +5.0`，`std=0.60`；
- `motion_anchor_ang_vel +1.0`，`std=1.50`。

### Foot tracking 合并

v5.10 的 `motion_foot_pos`、`dribbling_gait_foot_tracking` 和 `foot_distance` 在 S2 v5.12 中全部关闭，替换为一项：

- `s2_windowed_foot_tracking +1.0`，`std=0.30`；
- contact window 外正常跟踪 reference foot；
- contact window 内乘以 `0.30`，给真实触球留出偏离 reference 的空间。

### 球相关 reward 只保留四项

| Reward | 权重 | 当前判定 |
|---|---:|---|
| `s2_contact_proximity` | `+2.0` | 只在 event window 激活；球越接近冻结 reference foot 的目标区域，值越高，区域误差的 `std=0.12 m`。 |
| `s2_new_touch` | `+5.0` | event window 内出现接触上升沿、触球部位为指定踝、接触力 `≤22 N` 时只奖励一次；持续接触不重复得分。 |
| `s2_correct_side` | `+2.0` | new touch 发生时，以实际触球脚当前 yaw-local frame 判断左右；落在正确侧且不在 dead zone 才给 bonus。 |
| `s2_undesired_ball_contact` | `-6.0` | 新的非踝部触球记 1.0，错误脚触球记 0.5，因此实际分别为 `-6.0` 和 `-3.0`。 |

错误侧当前只拿不到 side bonus，不直接惩罚，也不 termination。

### 相对 v5.10 关闭的球 reward

以下 v5.10 项在 S2 v5.12 中不再生效：

- 球位置和推进：`face_ball`、`dynamic_proximity`、`ball_forward_progress`、`chase_ball`、`dribbling_velocity_tracking`、`approach_foot_ball`；
- 旧 CG 监督：`cg_contact_consistency`、`cg_premature_contact`、`cg_foot_consistency`、`cg_flow_release`、`cg_flow_progress`、`phase_graph_alignment`、`legal_foot_touch`；
- 防取巧 shaping：`stall_no_touch_penalty`、`ball_speed_excess`、`ball_coast_penalty`、`sustained_contact_penalty`、`ball_bounce_penalty`、`rapid_retouch_penalty`、`micro_contact_filter`、旧 `undesired_contact_penalty`、`support_ankle_roll`。

也就是说，v5.12 不再要求 reference 的完整球轨迹和触球后的 flow，只要求在稀疏 event 上完成真实触球。

## Termination 的变化

姿态严重异常、严重身体碰撞、`time_out` 和 `motion_clip_end` 等基础 termination 继续保留。相对 v5.10 的球和末端 termination 修改如下：

| 项目 | v5.10 | v5.12 |
|---|---|---|
| ball-lost 距离 | ball-pelvis XY 单帧超过 `1.0 m` | 超过 `1.10 m` 且连续 10 个环境步才结束；episode 前 10 步宽限 |
| ball-pelvis 速度差 | `>2.0 m/s` termination | 已关闭，`max_vel_divergence=0` |
| 固定长期未触球 | 连续固定步数无触球 termination | 已删除 |
| ankle/wrist reference height | 误差过大 termination | 已删除 `ee_body_pos` |
| missed contact | 无 | event window 结束后再等待 3 个环境步，仍无有效 new touch 则记 `missed_contact` |

S2-A 和 S2-B 会因 `missed_contact` 直接结束 episode；S2-C4、S2-C8 和 S2-full 当前只统计 miss，不因此 termination。错误脚、错误侧、轻微提前/延迟和目标区域误差本身都不直接 termination。

## Contact 数量课程

v5.12 增加了五个独立 task：

| 阶段 | Task ID | 每个 episode 的采样混合 | missed-contact termination |
|---|---|---|---|
| S2-A | `Tracking-CG-G1-Dribbling-RNN-unified-s2-local-reference` | 100% 单 contact | 开启 |
| S2-B | `Tracking-CG-G1-Dribbling-RNN-unified-s2-two-contact` | 30% 单 contact，70% 相邻双 contact | 开启 |
| S2-C4 | `Tracking-CG-G1-Dribbling-RNN-unified-s2-four-contact` | 20% / 30% / 50% 的 1 / 2 / 4 contact | 关闭，只记录 |
| S2-C8 | `Tracking-CG-G1-Dribbling-RNN-unified-s2-eight-contact` | 10% / 20% / 30% / 40% 的 1 / 2 / 4 / 8 contact | 关闭，只记录 |
| S2-full | `Tracking-CG-G1-Dribbling-RNN-unified-s2-full-contact` | 10% / 20% / 30% / 40% 的 1 / 2 / 4 / full clip | 关闭，只记录 |

双 contact 采样会在数据存在时同时覆盖 inside→outside 和 outside→inside transition。所有长度使用同一套 reward，长课程不会额外打开推进或下一触球 shaping。

当前代码只会在一个 task 内按上述比例自动随机采样；**S2-A → S2-B → S2-C4 → S2-C8 → S2-full 的阶段晋级尚未自动化**。成功率达到标准后，需要训练者切换 task，并从上一阶段 checkpoint 继续训练。80% 单触球成功率、75% correct-side、60% 双触球完成率等晋级门槛目前只是训练决策标准，不是代码里的自动 curriculum 条件。

## 指标、可视化和对照实验

v5.12 新增的训练指标包括：

- `s2_contact_success_rate`、`s2_missed_contact_rate`；
- `s2_correct_foot_rate`、`s2_correct_side_rate`；
- `s2_contact_timing_error`、`s2_contact_timing_abs_error`；
- `s2_target_region_distance`；
- `s2_wrong_foot_count`、`s2_wrong_side_count`、`s2_invalid_body_contact_count`；
- `s2_complete_2`、`s2_complete_4`、`s2_complete_8`。

`play_multi.py --show_s2_contact_regions` 会显示当前 event 的 reference foot frame 和左右目标区域边界。配合 `--diagnostic --diagnostic_stride 1`，诊断文件会额外保存 event id/frame、期望脚、期望侧、reference foot pose、球在 foot frame 中的 offset 和目标区域距离。

新增四个累计式对照 task：

- `...-unified-s2-ablation-motion`：只有 motion/reference-twist 基线；
- `...-unified-s2-ablation-time`：加入 contact timing，任一踝触球都可接受；
- `...-unified-s2-ablation-foot`：再要求指定脚，不要求左右侧；
- `...-unified-s2-ablation-side`：完整 timing + foot + side 目标。

## 当前 v5.12 已知问题

以下是 `diagnostic_20260807_140159.npz` 已暴露、但当前提交尚未修复的问题：

1. 短课程虽然选择了本 episode 的 first/last contact 来决定初始化和结束帧，但 event property 仍会暴露 pre-roll 中遇到的其他全局 contact window。结果是 S2-A 可能在目标 event 之前因上一个未选中的 event 触发 `missed_contact`。
2. S2 event state 依赖 `episode_length_buf == 0` 清理；在当前 manager 调用顺序下可能残留上一个 episode 的 tracked event，导致新 episode 提前 miss。
3. `max_touch_force=22 N` 对现有仿真偏严。该诊断中，窗口内 25 次接触上升沿只有 9 次低于此阈值，大量真实触球未被记为有效 new touch。
4. proximity 使用 reference foot 目标区域，而球本身又从该区域附近出生，因此即使实际脚仍离 reference 较远，也可能得到很高 proximity。

因此 v5.12 已完成课程、reward 和诊断框架，但当前 S2-A 结果不能直接当作策略能力结论。下一版应先屏蔽未选中的 event、在 resample 时显式重置 event state，并放宽或软化触球力阈值，再重新评估 foot tracking 和 proximity。

## 实现位置

- S2 配置和各阶段 task：[soccer_dribbling_env_cfg.py](source/whole_body_tracking/soccer/tasks/tracking/config/g1/soccer_dribbling_env_cfg.py)
- task 注册：[__init__.py](source/whole_body_tracking/soccer/tasks/tracking/config/g1/__init__.py)
- contact event、课程采样、球初始化和可视化：[commands_dribble_cg.py](source/whole_body_tracking/soccer/tasks/tracking/mdp/commands_dribble_cg.py)
- S2 reward、event state 和指标：[rewards_dribbling.py](source/whole_body_tracking/soccer/tasks/tracking/mdp/rewards_dribbling.py)
- ball-lost 和 missed-contact termination：[terminations.py](source/whole_body_tracking/soccer/tasks/tracking/mdp/terminations.py)
- playback 诊断：[play_multi.py](scripts/rsl_rl/play_multi.py)
