# v5.10 S2：参考条件下的物理盘带

任务：`Tracking-CG-G1-Dribbling-RNN-unified-s2-local-reference`。

S2 在 S1 的局部速度和动作模仿基线上加入真实物理球，并用示范中的 contact graph（CG）教策略在何时、用哪只脚、哪个 instep 区域触球。球从 reference 的首次接触点在 pelvis-local 坐标中出生，但始终由物理仿真驱动，不会吸附到参考球轨迹。

## Reward：保留基线，新增“按参考触球”

S2 保留 S1 的动作平滑、关节/碰撞约束和局部 root-twist 跟踪；为了给控球留出自由度，模仿项改为较软的 body/foot/gait/pelvis 姿态先验（包括非接触时的脚部跟踪）。下表列出继承或调整后的共同项；它们的公式与基础解释见 [S1 510.md](S1%20510.md)。

| 继承/调整的 reward | S2 配置 | 作用 |
|---|---|---|
| 局部根部速度 | `motion_anchor_lin_vel +5.0`（`std=0.60`）、`motion_anchor_ang_vel +1.0`（`std=1.50`） | 跟踪 reference 的 pelvis-local `[vx, vy, wz]`。 |
| 动作与步态先验 | `motion_body_pos/ori +0.72/+1.0`、`motion_body_lin_vel/ang_vel +0.3/+0.3`、`motion_foot_pos +0.55`、`dribbling_gait_foot_tracking +1.4`、pelvis quaternion `+2.0`、`foot_distance +0.35` | 保留动作风格和非接触时的行走步态，同时允许控球所需偏离。 |
| 稳定与安全 | `action_rate_l2 -0.10`、`waist_action_rate_l2 -0.25`、`joint_limit -10.0`、`undesired_contacts -0.10`、`pelvis_orientation -2.5`、上肢两项 `-0.05/-0.02` | 维持平滑、直立、关节合法且不以异常身体接触取巧。 |

S2 新增或显著改变的项如下：

| 层次 | 关键项 | 作用 |
|---|---|---|
| 球位置与推进 | `face_ball +2.5`、`dynamic_proximity +3.0`、`ball_forward_progress +7.5`、`chase_ball +2.0` | 让球保持在身体前方可控区域；真实触球后沿当前参考局部命令推进，踢远时主动追球。 |
| CG 接触监督 | `cg_contact_consistency +5.0`、`cg_foot_consistency +2.0`、`legal_foot_touch +5.5`、`cg_flow_release/progress +2.0/+0.5` | 要求接触时序、左右脚、inside/outside instep 与 reference 一致，并奖励一次正确触球之后的参考 flow。 |
| 错误接触与取巧抑制 | premature contact `-6.0`、持续/过快/过硬触球 `-6/-6/-4`、非踝接触 `-12.0`、球过快/滑行/弹跳 `-2.5/-2.2/-3.0`、停在球旁 `-5.5` | 防止抢拍触球、夹球、猛踢、身体碰球或只模仿不盘带。 |

### S2 新增 reward 说明

`dribbling_face_ball`：奖励球处于 pelvis-local 正前方。它给“先把球摆到可操作方位”提供连续信号，但不要求世界坐标朝向。

`dribbling_dynamic_proximity`：奖励球位于局部前方 `0.28–0.72 m`、横向接近 0 的走廊；低速或球已在走廊却不触球时会降权。因此策略不能只站在球旁累积 proximity 分数。

`dribbling_ball_forward_progress`：只在最近确有物理接触、机器人也在运动时，奖励球沿当前参考局部命令方向的有效推进，同时抑制横向速度占比过高的运动。它把“碰到球”变成“把球往正确方向带走”。

`dribbling_chase_ball`：球在命令前方且尚未接触时，奖励机器人沿命令方向追近球。它为一次触球稍大后重新接管球提供恢复信号。

`dribbling_cg_contact_consistency`：比较仿真接触 bit 与当前 reference contact bit；匹配即奖励，二者都不接触也算匹配。它是 S2 对接触节奏的主监督。

`dribbling_cg_premature_contact`：reference 标注为非接触帧却发生物理接触时惩罚。它与上一项配合，阻止策略提前蹭球或持续粘球来换取其他奖励。

`dribbling_cg_foot_consistency`：在标注接触帧，只有实际触球脚及 inside/outside instep side 都匹配 reference 才奖励。它把“按时碰到球”进一步约束为“按正确脚面碰到球”。

`dribbling_legal_foot_touch`：只对一次新发生的、力量不超过阈值的正确踝部 instep 触球奖励，持续接触不会逐帧重复得分。它鼓励离散而轻柔的控球触球。

`dribbling_cg_flow_release`：一次正确新触球会锁存该接触后 reference 的球流方向；在随后的短窗口中，若球以合理速度沿此方向释放，则给予奖励。它监督单次触球的“出球质量”。

`dribbling_cg_flow_progress`：从正确触球点开始，仅奖励沿锁存 flow 方向的历史最佳推进增量，倒退再前进不能反复刷分。它监督一次接触之后的净推进距离。

`dribbling_stall_no_touch_penalty`：球在近处、机器人速度很低且不触球时惩罚。它排除“把球放好后原地不动”的局部最优。

`dribbling_ball_speed_excess`：球的平面速度超过 `1.35 m/s` 后惩罚，抑制把盘带退化成一脚把球踢远。

`dribbling_ball_coast_penalty`：球仍在近身范围、无人接触却持续快速滑行时惩罚。它要求策略对近处失控球及时接管，而非看着球滑走。

`dribbling_sustained_contact_penalty`：接触连续超过 3 个环境步就惩罚，抑制用脚长时间顶住、夹住或拖住球。

`dribbling_ball_bounce_penalty`：触球期间球的竖直速度过大时惩罚，避免夹球、踩球带来的非期望弹跳。

`dribbling_rapid_retouch_penalty`：两次新轻触间隔小于 26 个环境步时惩罚，避免高频碎触或接触抖振。

`dribbling_micro_contact_filter`：对踝部触球力做 EMA 平滑后仍过大时惩罚；相比单帧阈值，它能稳定识别持续性硬触球。

`dribbling_undesired_contact_penalty`：膝、腕等非踝部触球受到强惩罚；踝部落在错误 instep side 时受到较小惩罚。它防止身体其他部位成为替代控球工具。

补充：代码中虽有 `dribbling_cg_foot_ball_distance_exp()`，但 S2 配置未创建它的 `RewTerm`，故它不是当前有效 reward。S2 也刻意关闭了 `ball_trapped` 和 `orbiting` 惩罚，因为它们会误伤参考动作。

## Termination

S2 的全部 termination 如下：

| 类型 | 条件 |
|---|---|
| 正常 time-out | 达到 `10 s`，或 reference clip 结束（`motion_clip_end`）。后者会同时 reset robot、ball 和 RNN。 |
| 姿态失败 | pelvis 高度误差 `>0.25 m`、projected-gravity Z 差 `>0.8`，或任一 ankle/wrist 的参考相对高度误差 `>0.25 m`。 |
| 失球 | 前 50 步宽限后，ball-pelvis XY 距离 `>1.0 m`，或二者 XY 速度差 `>2.0 m/s`。 |
| 长期未接触 | 前 50 步宽限后连续 50 步没有足够的 robot-ball 接触力；从 episode 开始完全不触球时约 `2 s` 终止。 |

接触时机或脚别错误通过 reward shaping 处理，不会直接终止；没有 terminal bonus/penalty 或 success termination。

## 阶段结论

S2 把 S1 的动作先验变成“按示范接触结构完成的物理盘带”：球必须可控、能推进，但触球时间、脚别、脚面和后续球流仍由 reference 规定。

## 实现位置

- 配置：[soccer_dribbling_env_cfg.py](source/whole_body_tracking/soccer/tasks/tracking/config/g1/soccer_dribbling_env_cfg.py)
- 盘带 reward：[rewards_dribbling.py](source/whole_body_tracking/soccer/tasks/tracking/mdp/rewards_dribbling.py)
- termination：[terminations.py](source/whole_body_tracking/soccer/tasks/tracking/mdp/terminations.py)
